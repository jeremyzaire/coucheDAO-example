CREATE TABLE product(
    product_id INT PRIMARY KEY NOT NULL,
    content VARCHAR(100),
);

CREATE TABLE product_hibernate(
    product_id INT PRIMARY KEY NOT NULL,
    content VARCHAR(100),
);

CREATE TABLE product_mybatis(
    product_id INT PRIMARY KEY NOT NULL,
    content VARCHAR(100),
);

CREATE TABLE product_springdata(
    product_id INT PRIMARY KEY NOT NULL,
    content VARCHAR(100),
);